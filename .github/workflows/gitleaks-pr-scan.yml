name: Gitleaks PR Secret Scan

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    uses: clouddrove/github-shared-workflows/.github/workflows/gitleaks-pr-scan.yml@7d78c7b4b3922e7577e222d4f48f1197c7d507c8 # pinned to latest
    secrets: inherit
